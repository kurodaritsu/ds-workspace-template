interface:
  display_name: "Ds Workspace Template"
  short_description: "Repo-specific patterns and workflows for ds-workspace-template"
  default_prompt: "Use the ds-workspace-template repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true